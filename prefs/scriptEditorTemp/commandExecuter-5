import pymel.all as pm

sel = pm.ls(sl=True)
srcdir = sel[0]
tgtdir = sel[1]

for c in srcdir.getChildren() :
    n = c.name()
    n = n.replace( srcdir.name(), '' )
    tgt = tgtdir.name() + n
    
    pm.select( c, tgt )
    Cas_massCopyWeight_python.Cas_massCopyWeight_cmd( 0.001, False )